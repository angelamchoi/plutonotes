<%- include('partials/header') %>
<body>
    <div class="navbar-fixed">
        <nav>
            <div class="nav-wrapper">
                <!-- <a href=""class="brand-logo left">plutonotes</a> -->
                <ul id="nav-mobile" class="left hide-on-med-and-down">
                    <li><a href="../home">Home</a></li>
                    <li><a href="/users/../users/mynotes">My notes</a></li>
                    <li><a href="/users/../users/create">Create notes</a></li>
                    <li><a href="/users/publicnotes">Public notes</a></li>
                  </ul>
                <ul class="right">
                    <li>
                        <% if (user) { %>
                            <a href="/logout"><i class="material-icons left">trending_flat</i>Log Out</a>
                            <% } else { %>
                                <a href="/auth/google"><i class="material-icons left">vpn_key</i>Login with Google</a>
                                <% } %>
                    </li>
                </ul>
            </div>
        </nav>
    </div>

  <div class ="create-notes">
    <h2> View Mode</h2>
      <!-- Switch -->
      <div class="switch">
        <label> 
          light mode
        <input type="checkbox">
        <span class="lever"></span>
          dark mode
        </label>
      </div>
<br></br>
     <!-- Form -->
  <form action ="/users/mynotes/<%=note._id %>?_method=GET" class= "view-form" method="POST">
    <div class="row">
      <div class="col s12 m6">
        <div class="card blue-grey darken-1">
          <div class="card-content white-text">
            <span class="card-title"><%=note.title %></span>
              <p><%= note.description %> </p>
              <p><%=note.date %></p>
              <p><%=note.content %></p>
          </div>
        </div>
       </div>
      </div>
     </form>   
  <!-- Form for share publicly -->
  <form action ="/plublicnotes/<%=note._id %>?_method=GET" class= "post-form" method="POST">
    <div class="form-group">
      <div class="card-action">
        <h6> If you think your notes are gucci, feel free to share it with the world! 😎🌎</h6>
          <input type="submit" value="Share publicly">
            </div>
          </div>
  </form>
</body>
</html>


